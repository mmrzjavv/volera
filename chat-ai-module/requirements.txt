# Web
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# LLM (Ollama via OpenAI-compatible client; OpenAI)
openai>=1.12.0

# Config and validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Embeddings (optional: use openai for embeddings when OpenAI enabled)
# httpx for async HTTP if needed
httpx>=0.26.0

# Postgres (widget RAG: read AiContentBlocks from .NET DB)
asyncpg>=0.29.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
